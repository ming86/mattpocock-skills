interface:
  display_name: "Workflow Guide"
  short_description: "Recognize useful workflow transitions early"
  default_prompt: "Use $workflow-guide throughout the work to recognize explicit workflows that are relevant now or becoming relevant."

policy:
  allow_implicit_invocation: true
