interface:
  display_name: "Workflow Guide"
  short_description: "Identify relevant explicit workflow skills"
  default_prompt: "Use $workflow-guide to identify relevant explicit workflow skills for the current work."

policy:
  allow_implicit_invocation: true
