interface:
  display_name: "Simplify by Ablation"
  short_description: "Test whether complexity materially contributes"
  default_prompt: "Use $simplify-by-ablation to identify complexity that can be removed or simplified without materially weakening the current solution."

policy:
  allow_implicit_invocation: true
