interface:
  display_name: "Implementation Review"
  short_description: "Review an implementation against intent and repository fit"
  default_prompt: "Use $implementation-review to review this bounded implementation against the requested outcome and repository fit."

policy:
  allow_implicit_invocation: false
