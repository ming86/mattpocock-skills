interface:
  display_name: "Grill With Docs"
  short_description: "Clarify important decisions using project evidence"
  default_prompt: "Use $grill-with-docs to resolve the important decisions that must be settled before dependent work continues."

policy:
  allow_implicit_invocation: false
