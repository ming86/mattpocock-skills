interface:
  display_name: "Grill With Docs"
  short_description: "Clarify material decisions before dependent work"
  default_prompt: "Use $grill-with-docs to resolve the material decisions that must be settled before dependent work continues."

policy:
  allow_implicit_invocation: false
