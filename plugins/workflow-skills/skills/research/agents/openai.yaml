interface:
  display_name: "Research"
  short_description: "Investigate technical questions from primary sources"
  default_prompt: "Use $research to investigate this technical or project question using high-trust primary sources."

policy:
  allow_implicit_invocation: true
