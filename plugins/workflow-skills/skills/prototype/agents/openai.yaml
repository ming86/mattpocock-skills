interface:
  display_name: "Prototype"
  short_description: "Answer uncertainty with a small experiment"
  default_prompt: "Use $prototype to build the smallest throwaway experiment that can answer this question."

policy:
  allow_implicit_invocation: false
