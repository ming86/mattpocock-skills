interface:
  display_name: "Grilling"
  short_description: "Clarify material decisions with focused questions"
  default_prompt: "Use $grilling to clarify the material decisions blocking the next step."

policy:
  allow_implicit_invocation: true
