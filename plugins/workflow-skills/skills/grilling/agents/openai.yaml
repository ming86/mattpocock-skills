interface:
  display_name: "Grilling"
  short_description: "Clarify important decisions with focused questions"
  default_prompt: "Use $grilling to clarify the important decisions blocking the next step."

policy:
  allow_implicit_invocation: true
