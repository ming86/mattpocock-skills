interface:
  display_name: "To Tickets"
  short_description: "Split work into coherent dependency-aware units"
  default_prompt: "Use $to-tickets to decompose this plan or spec into coherent, dependency-aware work units."

policy:
  allow_implicit_invocation: false
