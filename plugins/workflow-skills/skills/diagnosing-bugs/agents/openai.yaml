interface:
  display_name: "Diagnosing Bugs"
  short_description: "Find root causes with focused evidence loops"
  default_prompt: "Use $diagnosing-bugs to diagnose this bug with the smallest credible evidence loop, and fix it only if the current task authorizes a correction."

policy:
  allow_implicit_invocation: true
