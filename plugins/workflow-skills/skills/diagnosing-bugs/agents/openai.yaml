interface:
  display_name: "Diagnosing Bugs"
  short_description: "Find root causes with focused evidence"
  default_prompt: "Use $diagnosing-bugs to diagnose this bug with focused evidence, and fix it only when fixing it is part of the current task."

policy:
  allow_implicit_invocation: true
