interface:
  display_name: "Diagnosing Bugs"
  short_description: "Find root causes with focused evidence"
  default_prompt: "Use $diagnosing-bugs to diagnose this bug with focused evidence, and fix it only if the current task authorizes a fix."

policy:
  allow_implicit_invocation: true
