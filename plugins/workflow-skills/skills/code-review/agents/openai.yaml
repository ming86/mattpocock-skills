interface:
  display_name: "Code Review"
  short_description: "Review changes for intent and repository fit"
  default_prompt: "Use $code-review to review this bounded change for requested-outcome conformance and repository fit."

policy:
  allow_implicit_invocation: true
