interface:
  display_name: "To Spec"
  short_description: "Turn current understanding into an implementation spec"
  default_prompt: "Use $to-spec to turn the current decisions and project context into an implementation specification later work can rely on."

policy:
  allow_implicit_invocation: false
