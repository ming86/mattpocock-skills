interface:
  display_name: "To Spec"
  short_description: "Turn settled understanding into a stable spec"
  default_prompt: "Use $to-spec to turn the settled decisions and project context into a stable implementation specification."

policy:
  allow_implicit_invocation: false
