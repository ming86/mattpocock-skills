interface:
  display_name: "Implement"
  short_description: "Implement a well-scoped work item"
  default_prompt: "Use $implement to implement this well-scoped work item from the current plan, spec, or ticket."

policy:
  allow_implicit_invocation: false
