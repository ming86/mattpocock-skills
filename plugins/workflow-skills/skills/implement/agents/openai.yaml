interface:
  display_name: "Implement"
  short_description: "Implement a bounded, established unit of work"
  default_prompt: "Use $implement to implement this bounded work item from the established plan, spec, or ticket."

policy:
  allow_implicit_invocation: false
